// Fill these in once you've done the two setup steps in README.md: created
// the Supabase project (or reused the personal app's one) and deployed
// server.js as its own Render service.
//
// None of these three values are secret. The Supabase "anon" key is
// specifically designed to be public — it's what every Supabase app ships
// to the browser — and it's Row Level Security (self-service/schema.sql),
// not this key, that actually keeps one person's data away from another's.
// Edit this file directly on GitHub, the same way passcode.js works for the
// personal app: github.com/jackkay22/stride/blob/main/self-service/config.js
window.STRIDE_SS_CONFIG = {
  SUPABASE_URL: 'https://tttipmmgxszlmmjpqjus.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_wKCh5htNNPr1UOExSCp8Yg_bdZuvaPN',
  BACKEND_URL: 'https://stride-1-kqq4.onrender.com',
};
