{
  "name": "stride-self-service-backend",
  "version": "1.0.0",
  "description": "Backend for the self-service (multi-user) Stride app — separate from Jack's personal backend on purpose. See README.md.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
